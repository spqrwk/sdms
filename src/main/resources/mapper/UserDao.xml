<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.sdms.dao.UserDao">

    <resultMap type="com.github.sdms.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userCode" column="user_code" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
    </resultMap>

    <!--根据code查询单个-->
    <select id="queryByCode" resultMap="UserMap">
        select
          id, user_code, password, username
        from sdmsdb.sdms_user
        where user_code = #{userCode}
    </select>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, user_code, password, username
        from sdmsdb.sdms_user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        SELECT
            *
        FROM
            sdms_user su
        <where>
            <if test="userCode != null and userCode !=''">
                AND su.user_code LIKE CONCAT('%', #{userCode}, '%')
            </if>
            <if test="username != null and username != ''">
                AND su.username LIKE CONCAT('%', #{username}, '%')
            </if>
        </where>
        <if test="startIndex != null and pageSize != null">
            LIMIT #{startIndex}, #{pageSize}
        </if>
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, user_code, password, username
        from sdmsdb.sdms_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userCode != null and userCode != ''">
                and user_code = #{userCode}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
        </where>
    </select>

    <select id="countByLimit" resultType="java.lang.Integer">
        SELECT
            count(1)
        FROM
            sdms_user su
        <where>
            <if test="userCode != null and userCode !=''">
                AND su.user_code LIKE CONCAT('%', #{userCode}, '%')
            </if>
            <if test="username != null and username != ''">
                AND su.username LIKE CONCAT('%', #{username}, '%')
            </if>
        </where>
        <if test="startIndex != null and pageSize != null">
            LIMIT #{startIndex}, #{pageSize}
        </if>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sdmsdb.sdms_user(user_code, password, username)
        values (#{userCode}, #{password}, #{username})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sdmsdb.sdms_user(user_code, password, username)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userCode}, #{entity.password}, #{entity.username})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sdmsdb.sdms_user(user_code, password, username)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userCode}, #{entity.password}, #{entity.username})
        </foreach>
        on duplicate key update
        user_code = values(user_code) , password = values(password) , username = values(username)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sdmsdb.sdms_user
        <set>
            <if test="userCode != null and userCode != ''">
                user_code = #{userCode},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sdmsdb.sdms_user where id = #{id}
    </delete>

</mapper>